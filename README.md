cat > README.md <<EOF
# Custom Memory Allocator (vmalloc)

A simple custom memory allocator written in C.
Implements my_malloc, my_free, my_calloc, my_realloc and a small interactive shell (vmalloc_shell.c).
EOF

